fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1

# --- Kronos side-selection (app/kronos_signal.py) ---
pandas==2.2.2
numpy==1.26.4
torch==2.4.1              # CPU wheel is fine for Kronos-small/base; swap for a CUDA build if KRONOS_DEVICE=cuda
huggingface_hub==0.33.1
einops==0.8.1
tqdm==4.67.1
safetensors==0.6.2

# Kronos model code is vendored in model/ from:
# https://github.com/shiyu-coder/Kronos
